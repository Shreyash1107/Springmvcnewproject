<%@ page language="java" contentType="text/html; charset=ISO-8859-1" pageEncoding="ISO-8859-1"%>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Batch</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="<c:url value='/resources/CSS/updatedept.css' />" rel="stylesheet" />
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Admin</a>
        </div>
    </nav>

    <!-- Sidebar -->
    <div class="sidebar">
        <h3 class="text-center">Admin Dashboard</h3>
        <a href="department">Department Module</a>
        <a href="btchmodel">Batch Module</a>
        <a href="alumni">Alumni Module</a>
        <a href="#">Event Module</a>
        <a href="#">Feedback Module</a>
        <a href="logout">Log out</a>
    </div>

    <!-- Main Content -->
    <div class="content">
        <h4 class="text-center">Update Batch Module</h4>
        <form name="frm" id="frm" action="finalupdatebatch" method="POST">
            <div class="row mb-3">
                <!-- Department ID Field (Hidden) -->
                <input type="hidden" name="dept_id" value="${dept_id}">

                <!-- Current Department Name Field (Read-Only) -->
                <div class="col-md-4">
                    <label for="currentDeptName" class="form-label mt-4">Current Department Name</label>
                    <input type="text" class="form-control mt-2" id="currentDeptName" name="currentDeptName" value="${currentDeptName}" readonly>
                </div>

                <!-- Updated Department Name Field (Dropdown) -->
                <div class="col-md-4">
                    <label for="updatedDeptName" class="form-label mt-4">Updated Department Name</label>
                    <select class="form-control mt-2" id="updatedDeptName" name="updatedDeptName">
                        <c:forEach var="dept" items="${departmentsList}">
                            <option value="${dept.dept_id}">${dept.dept_name}</option>
                        </c:forEach>
                    </select>
                </div>

                <!-- Batch Year Field -->
                <div class="col-md-4">
                    <label for="txtBatch" class="form-label mt-4">Batch Year</label>
                    <input type="text" name="batch_year" id="txtBatch" class="form-control mt-2" value="${batch_year}" placeholder="Enter Batch Year" autocomplete="off">
                </div>

                <!-- Update Button -->
                <div class="col-md-12 d-flex justify-content-end mt-4">
                    <input type="hidden" name="bid" value="${bid}">
                    <input type="submit" name="s" id="btn" value="Update Batch" class="btn btn-info font-weight-bold"/>
                </div>
            </div>
        </form>

        <!-- Alert Message -->
        <c:if test="${not empty m}">
            <div class="alert alert-primary mt-4">${m}</div>
        </c:if>

        <!-- Batch Table -->
        <div class="mt-5">
            <table id="data" class="table table-striped table-dark">
                <thead>
                    <tr>
                        <th scope="col">Batch ID</th>
                        <th scope="col">Batch Year</th>
                        <th scope="col">Department Name</th>
                        <th scope="col">Update</th>
                        <th scope="col">Delete</th>
                    </tr>
                </thead>
                <tbody>
                    <c:forEach var="batch" items="${v}">
                        <tr>
                            <td>${batch.bid}</td>
                            <td>${batch.batch_year}</td>
                            <td>${batch.dept_name}</td>
                            <td>
                                <a href="updatebatches?bid=${batch.bid}&batch_year=${batch.batch_year}&dept_id=${batch.dept_id}" class="btn btn-warning" role="button">Update</a>
                            </td>
                            <td>
                                <a href="deletebatch?bid=${batch.bid}" class="btn btn-danger" role="button">Delete</a>
                            </td>
                        </tr>
                    </c:forEach>
                </tbody>
            </table>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
