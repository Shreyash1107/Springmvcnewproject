function validateName() {
    var name = document.forms["frm"]["Name"].value;
    var nameError = document.getElementById("everror");
    var namePattern = /^[A-Za-z\s]+$/;
    if (name === "") {
        everror.innerHTML = "Event Name is required.";
        return false;
    } 
    else if (/^\s/.test(name)) {
        everror.innerHTML = "Name should not start with spaces.";
        return false;
    }
    else if (!namePattern.test(name.trim())) {
        everror.innerHTML = "Name should contain only letters and spaces.";
        return false;
    } 
    else {
        everror.innerHTML = "";
        return true;
    }
}
