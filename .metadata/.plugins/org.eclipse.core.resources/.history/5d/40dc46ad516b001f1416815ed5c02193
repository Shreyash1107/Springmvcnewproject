package org.service;

import org.model.FeedbackModel;
import org.repository.Feedbackrepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

@Service("feedservice")
public class FeedbackServiceImpl implements FeedbackService {

    @Autowired
    Feedbackrepository feedrepo;

	/*
	 * @Autowired EmailServiceImpl emailService;
	 */

    public boolean issubmitFeedback(FeedbackModel fmodel) {
        boolean isSubmitted = feedrepo.issubmitFeedback(fmodel);
		/*
		 * if (isSubmitted) { String adminEmail = "shreyunkawane@gmail.com"; // Replace
		 * with actual admin email String feedbackDetails = fmodel.getDetails() +
		 * "\nRating: " + fmodel.getRating();
		 * emailService.sendFeedbackNotification(adminEmail, feedbackDetails); } return
		 * isSubmitted;
		 */
        return isSubmitted;
    }
}
